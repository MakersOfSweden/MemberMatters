# Test-only dependencies. Kept out of requirements.txt so the runtime Docker
# image doesn't carry the test toolchain.
#
#   pip install -r memberportal/requirements-dev.txt
-r requirements.txt

# pytest-django 4.8 is the last line that still declares Django 3.2 support;
# newer releases require Django >= 4.2. Bump only alongside the Django upgrade.
pytest~=8.3
pytest-django~=4.8.0
factory-boy~=3.3
pytest-cov~=5.0
